import { LabelcolorDirective } from './labelcolor.directive';

describe('LabelcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelcolorDirective();
    expect(directive).toBeTruthy();
  });
});
